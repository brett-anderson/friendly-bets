<div class="dashboard-container">
  <header>
    {{profile-container}}
    {{#link-to 'dashboard'}}<h2>Dashboard</h2>{{/link-to}}
  </header>
  <div class="left-container">
    {{chat-container messages=model.messages newMessage=(action 'newMessage') }}
  </div>
  <div class="dashboard">
    {{outlet}}
  </div>
</div>
